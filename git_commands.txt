 
  279  git add .
  284  git commit -m "my first ruby and git experitment"
  291  git status
  292  git branch
  294  git push origin master



  postgres commands

  sudo service postgresql start		=> start postgres server
  sudo -u postgres psql postgres 	=> start postgres prompt
  \deu+ 							=> show list of user
  \du 								=> show all users
  \c dbname  						=> select database

select column_name from information_schema.columns where table_name = 'student';